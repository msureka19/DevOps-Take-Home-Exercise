apiVersion: v1
kind: Pod
metadata:
  creationTimestamp: null
  labels:
    run: php
  name: php
spec:
  containers:
  - image: php
    name: php
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
  containers:
  - image: nginx
    name: nginx
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}